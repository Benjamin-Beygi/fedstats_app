% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{fed_logistic_newton}
\alias{fed_logistic_newton}
\title{Federated logistic regression via distributed Newton-IRLS}
\usage{
fed_logistic_newton(
  servers,
  formula,
  max_iter = 50,
  tol_score = 1e-08,
  tol_loglik = 1e-10,
  robust_cluster = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{servers}{List of server objects.}

\item{formula}{An R formula with a binary (0/1) outcome.}

\item{max_iter}{Integer. Maximum Newton iterations (default \code{50}).}

\item{tol_score}{Numeric. Score-norm convergence threshold (default
\code{1e-8}).}

\item{tol_loglik}{Numeric. Log-likelihood change convergence threshold
(default \code{1e-10}).}

\item{robust_cluster}{Logical. Compute cluster-robust SEs when
\code{length(servers) >= 2} (default \code{TRUE}).}

\item{verbose}{Logical. Print Newton iteration progress (default
\code{FALSE}).}
}
\value{
A list with \code{coefficients}, \code{se}, \code{vcov},
  \code{logLik}, \code{N}, \code{iterations}, \code{converged}, and
  optionally \code{se_robust}, \code{vcov_robust}, \code{clusters}.
}
\description{
Fits a logistic regression model by iteratively aggregating the gradient
and Hessian of the binomial log-likelihood across sites. Converges to
the same coefficients as fitting on the full pooled dataset.
}
\details{
Optionally computes cluster-robust (sandwich) standard errors treating
each site as one cluster.
}
